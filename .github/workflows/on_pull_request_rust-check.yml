name: "[rust] Pull Request"

on:
  pull_request:
    branches:
      - main
      - "issue/#*"

permissions:
  contents: read

jobs:
  rust:
    name: Call Rust check
    uses: ./.github/workflows/_rust-check.yml
